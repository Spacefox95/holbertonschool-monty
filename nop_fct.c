#include "monty.h"

/**
 * nop_fct - fct that does nothing
 * @stack: the stack
 * @line_number: line number
 */

void nop_fct(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
